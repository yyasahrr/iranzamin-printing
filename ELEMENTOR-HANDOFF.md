# راهنمای انتقال چاپ روشن به المنتور

این رابط به‌صورت بخش‌بندی‌شده در `src/App.tsx` و با استایل‌های مستقل در `src/index.css` ساخته شده است. تصاویر نیز همگی داخل `public/images` هستند؛ بنابراین وابستگی به سرویس تصویر خارجی ندارد.

## ساختار پیشنهادی ویجت

1. خروجی `npm run build` را به‌عنوان asset ویجت اختصاصی وردپرس enqueue کنید.
2. در متد `render` ویجت یک ریشه با شناسه `root` قرار دهید.
3. کنترل‌های متنی و تصاویر Elementor را به داده‌های ثابت ابتدای `src/App.tsx` متصل کنید.
4. برای جلوگیری از تداخل چند نمونه ویجت، هنگام تبدیل نهایی شناسه ریشه را با شناسه یکتای `$this->get_id()` بسازید.

## قرارداد ثبت سفارش

در پایان ویزارد، رویداد زیر روی `window` منتشر می‌شود:

```js
window.addEventListener("chap-roshan:order-created", function (event) {
  const order = event.detail;
  // order را با wp_ajax، Store API ووکامرس یا Edge Function ارسال کنید.
});
```

شیء سفارش شامل شناسه، محصول، مشخصات چاپ، اطلاعات مشتری، نام فایل، قیمت تخمینی و زمان ثبت است. تا پیش از اتصال بک‌اند، سفارش‌ها برای دموی کامل در `localStorage` نگهداری می‌شوند.

## اتصال ووکامرس

برای ووکامرس، پس از دریافت رویداد یک محصول متغیر یا محصول سفارشی را با قیمت محاسبه‌شده به سبد اضافه کنید و مشخصات چاپ را در cart item data قرار دهید. قیمت نهایی باید در سمت سرور دوباره محاسبه شود و نباید به مقدار مرورگر اعتماد شود.

## اتصال Supabase

جدول `print_orders` را با ستون‌های `id`، `product`، `details`، `customer`، `file_name`، `estimated_price` و `created_at` بسازید. فایل واقعی را جداگانه در Storage بارگذاری کنید و فقط مسیر امن فایل را در سفارش ذخیره کنید. کلید service role هرگز نباید در کد فرانت‌اند قرار بگیرد.

## نکات سازگاری

استایل‌ها کتابخانه UI جداگانه ندارند و فقط آیکن‌ها و حرکت‌های رابط از `lucide-react` و `framer-motion` استفاده می‌کنند. برای نسخه کاملاً بدون React در المنتور می‌توان همین کلاس‌های HTML و متغیرهای CSS را حفظ کرد و منطق چهار مرحله را با JavaScript ساده بازنویسی کرد.

---

## احراز هویت با کد OTP

مسیر `/login` یک صفحه ورود/ثبت‌نام دو مرحله‌ای دارد:

1. **مرحله شماره موبایل** – کاربر شماره ۱۱ رقمی خود را وارد می‌کند (با `09` شروع شود).
2. **مرحله کد OTP** – شش خانه مجزا برای ورود کد ۶ رقمی نمایش داده می‌شود. در دمو هر کد ۶ رقمی پذیرفته می‌شود.

پس از تأیید، پروفایل کاربر در `localStorage` ذخیره و کاربر به `/profile` هدایت می‌شود.

**اتصال به بک‌اند واقعی:** در فایل `src/pages/LoginPage.tsx` دو تابع `requestOtp` و `verifyOtp` وجود دارند. در نسخه دمو با `setTimeout` شبیه‌سازی شده‌اند. برای تولید:

- در `requestOtp`: به‌جای `setTimeout` یک درخواست HTTP به API ارسال کد بزنید.
- در `verifyOtp`: به‌جای `setTimeout` کد را به API تأیید بفرستید و پاسخ را با `login(profile)` از `AuthContext` ادامه دهید.

همچنین پروژه با `react-router-dom` برای مدیریت مسیرها ساخته شده است. روت‌ها در `src/main.tsx` تعریف شده‌اند.

## پنل کاربری

مسیر `/profile` پس از ورود در دسترس است و شامل سه تب است:

- **سفارش‌ها** – لیست سفارش‌های کاربر از `localStorage` فیلترشده با شماره موبایل.
- **اطلاعات** – فرم ویرایش نام، ایمیل و مشاهده شماره موبایل و تاریخ عضویت.
- **تنظیمات** – تغییر تم روشن/تیره، پیگیری خودکار و خروج از حساب.

ناوبری هدر سایت (`src/App.tsx`) هنگام ورود کاربر دکمه «ورود» را با یک بج پروفایل و نام کاربر جایگزین می‌کند.

**قرارداد رویداد احراز هویت:**

```js
// پس از ورود موفق
window.dispatchEvent(new CustomEvent("chap-roshan:auth-login", { detail: user }));
// `user` شامل { name, phone, email, registeredAt }
```

این رویداد به المنتور امکان می‌دهد وضعیت ورود کاربر را در سایر ویجت‌ها منعکس کند.